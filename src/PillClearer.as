function PillClearer() {
    this.clear = function(arrPills) {
        var pillTemp;
        for ( var i = 0 ; i < arrPills.length; i ++ ) {
            pillTemp = arrPills[i];
        }
    }
}